<%= form_with(model: attachment) do |form| %>
  <% if attachment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
        <% attachment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :attach_title, style: "display: block" %>
    <%= form.text_field :attach_title %>
  </div>

  <div>
    <%= form.label :attach_content, style: "display: block" %>
    <%= form.text_field :attach_content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
